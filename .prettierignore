dist
node_modules
.github
.changeset

# CMS-managed (Sitepins) - not hand-formatted, don't enforce house style
.sitepins
public/.well-known
src/data/posts
src/data/games
src/data/apps

# Search engine ownership-verification files - the content is fixed by the
# search engine and must be served byte-for-byte, so never reformat them
public/google*.html
public/naver*.html

# Web app build artifacts, pushed here by each app repo's own CI - generated
# code, never hand-formatted
public/board-game-dice
public/physics-lotto